"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.fetchAsyncGenerationResult = void 0;
const axios_1 = __importDefault(require("axios"));
const util_1 = require("../../util");
const error_1 = require("../../error");
const Util = __importStar(require("../../util"));
const RESOURCE = 'results';
var Endpoint;
(function (Endpoint) {
    Endpoint["FETCH_ASYNC_GENERATION_RESULT"] = "";
})(Endpoint || (Endpoint = {}));
/**
 * Fetch the result of an async generation by ID
 * Results are stored for 24 hours after generation
 *
 * @param id - The ID of the generation to fetch (64 characters)
 * @returns A promise resolving to the generation result
 */
async function fetchAsyncGenerationResult(...args) {
    const [id] = args;
    const response = await axios_1.default.get(Util.makeUrl(util_1.APIVersion.V2_BETA, RESOURCE, Endpoint.FETCH_ASYNC_GENERATION_RESULT) + `/${id}`, {
        validateStatus: undefined,
        headers: {
            ...this.authHeaders,
            Accept: 'application/json; type=image/png',
        },
    });
    if (response.status === 200) {
        return Util.processContentResponse(response.data, Util.DEFAULT_OUTPUT_FORMAT, 'v2beta_fetch_async_result');
    }
    else if (response.status === 202 &&
        typeof response.data.id === 'string' &&
        response.data.status === 'in-progress') {
        const { id, status } = response.data;
        return { id, status };
    }
    throw new error_1.StabilityAIError(response.status, 'Failed to fetch generation result', response.data);
}
exports.fetchAsyncGenerationResult = fetchAsyncGenerationResult;
//# sourceMappingURL=results.js.map